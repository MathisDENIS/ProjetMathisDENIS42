<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projetmathisdenis42">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".injection.EsieaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_bx"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.main.detailActivity"></activity>
        <activity android:name=".presentation.main.createActivity" />
        <activity android:name=".presentation.main.recyListActivity" />
        <activity android:name=".presentation.main.ListAdapter" />
        <activity android:name=".presentation.main.DetailActivity" />
        <activity android:name=".presentation.main.ListActivity" />
        <activity android:name=".presentation.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>